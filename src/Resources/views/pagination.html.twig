{% if pagination.total > 1 %}
{% spaceless %}
    <div class="pagination">
        <nav>
            {% if pagination.first %}
                <a href="{{ pagination.first.link }}" title="{{ 'pagination.first_page'|trans }}">&laquo;</a>
            {% endif %}
            {% if pagination.prev %}
                <a href="{{ pagination.prev.link }}" title="{{ 'pagination.previous_page'|trans }}">&larr;</a>
            {% endif %}
            {% for item in pagination %}
                {% if item.current %}
                    <span title="{{ 'pagination.current_page'|trans }}">{{ item.page }}</span>
                {% else %}
                    <a href="{{ item.link }}" title="{{ 'pagination.page_number'|trans({'%page%': item.page}) }}">{{ item.page }}</a>
                {% endif %}
            {% endfor %}
            {% if pagination.next %}
                <a href="{{ pagination.next.link }}" title="{{ 'pagination.next_page'|trans }}">&rarr;</a>
            {% endif %}
            {% if pagination.last %}
                <a href="{{ pagination.last.link }}" title="{{ 'pagination.last_page'|trans }}">&raquo;</a>
            {% endif %}
        </nav>
    </div>
{% endspaceless %}
{% endif %}
